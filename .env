# Optional Telegram notifications
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional OpenAI key for AI layer
OPENAI_API_KEY=

# ── ActionLoop (Vercept-level settings) ──────────────────────────────────────
# Max steps per adaptive run. 0 = unlimited. Default: 50 (was 12).
ALOOP_MAX_STEPS=50
# Per-step timeout in seconds. Default: 120 (was 60).
ALOOP_TIMEOUT=120
